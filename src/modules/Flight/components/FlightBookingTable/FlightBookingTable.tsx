import React from "react";

const FlightBookingTable: React.FC = () => {
  return (
    <React.Fragment>
      <p>
        FlightBookingTable module is currently under development. Stay tuned for
        updates! 🚧🚩
      </p>
    </React.Fragment>
  );
};

export default FlightBookingTable;
